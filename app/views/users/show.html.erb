<!DOCTYPE html>
<html>
<style>
  .button {
    transition: background-color 0.3s, color 0.3s; /* スムーズなトランジション */
  }

  .button:hover {
    background-color: #f0f0f0; /* ホバー時の背景色 */
    color: #333; /* ホバー時の文字色 */
  }

  .button:active {
    background-color: #e0e0e0; /* クリック時の背景色 */
    color: #111; /* クリック時の文字色 */
  }
</style>

<%= render "shared/header" %>
<p style="color: green"><%= notice %></p>

<body class="bg-[#FFF5E8] flex flex-col h-screen overflow-auto">
  <div class="flex-grow p-4 flex-col md:flex-row" style="display: flex; flex-direction: column;">  
    <div class="actions mb-4 flex mr-5">
      <% breadcrumb :users_show %>
      <%= breadcrumbs separator: " &rsaquo; " %>
    </div>
    <main class="flex-grow p-4 flex flex-col items-center justify-center">
      <div class="w-full max-w-md">
        <!-- 入力内容を白いメモ風の背景で囲む -->
        <div class="bg-white rounded p-8 mb-8">
          <div class="bg-white p-4 w-80 mb-3 flex justify-between items-center">
            <span class="text-lg">ユーザー名： <%= current_user.name %></span>
            <%= link_to content_tag(:i, '', class: 'fas fa-pencil-alt'), edit_user_path(current_user.id), class: "text-black" %>
          </div>
          <div class="bg-white p-4 w-80 flex justify-between items-center">
            <span class="text-lg">メールアドレス： <%= current_user.email %></span>
            <%= link_to content_tag(:i, '', class: 'fas fa-pencil-alt'), edit_user_path(current_user.id), class: "text-black" %>
          </div>
        </div>  

        <div class="actions flex justify-center">
          <%= link_to "パスワード変更", edit_user_registration_path, class: "bg-white text-black flex items-center px-4 py-2 rounded shadow button" %> 
        </div> 
      </div>  
    </main>
  </div>  
</body>

<!-- Font AwesomeのCDNを読み込む -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

<%= render "shared/footer" %>

</html>

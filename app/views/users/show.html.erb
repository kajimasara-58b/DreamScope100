<!DOCTYPE html>
<html>
<style>
  .button {
    transition: background-color 0.3s, color 0.3s; /* スムーズなトランジション */
  }

  .button:hover {
    background-color: #f0f0f0; /* ホバー時の背景色 */
    color: #333; /* ホバー時の文字色 */
  }

  .button:active {
    background-color: #e0e0e0; /* クリック時の背景色 */
    color: #111; /* クリック時の文字色 */
  }
</style>

<%= render "shared/header" %>
<p style="color: green"><%= notice %></p>

<body class="bg-[#FFF5E8] flex flex-col h-screen overflow-auto">

  <main class="flex-grow p-4 flex flex-col items-center justify-center">
    <h2 class="text-3xl mb-7"><i class="fas fa-user mr-2"></i>ユーザー情報</h2>

    <div class="bg-white p-4 w-80 mb-5">
      <h2 class="text-lg mb-2">ユーザー名： <%= current_user.name %></h2>
    </div>
    <div class="bg-white p-4 w-80 mb-5">
      <h2 class="text-lg mb-2">メールアドレス： <%= current_user.email %></h2>
    </div>

    <div class="actions flex justify-center">
      <%= link_to "編集", edit_user_registration_path, class: "bg-white text-black flex items-center px-4 py-2 rounded shadow button" %> 
    </div>
  </main>
</body>

<!-- Font AwesomeのCDNを読み込む -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

<%= render "shared/footer" %>

</html>
